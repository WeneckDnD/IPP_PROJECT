class Box : Integer {
}
class Main : Integer {
  run [|
    "true"
    x := 9.
    r := x identicalTo: x.
    _ := (r asString) print.
    "false"
    y := 9.
    r := x identicalTo: y.
    _ := (r asString) print.
    "true"
    s := 'z'.
    r := s identicalTo: s.
    _ := (r asString) print.
    "true TODO: FIX now false"
    r := nil identicalTo: nil.
    _ := (r asString) print.
    "true"
    o := Box new.
    r := o identicalTo: o.
    _ := (r asString) print.
    "false"
    p := Box new.
    r := o identicalTo: p.
    _ := (r asString) print.
    "TODO FIX: now false"
    r := true identicalTo: true.
    _ := (r asString) print.
    "TODO FIX: now false"
    r := false identicalTo: false.
    _ := (r asString) print.
  ]
}
