class Main : Integer {
  run [|
    "true"
    r := false.
    r := r not.
    _ := (r asString) print.

    "false"
    r := true not.
    _ := (r asString) print.

    "true"
    r := (true not) not.
    _ := (r asString) print.
  ]
}
